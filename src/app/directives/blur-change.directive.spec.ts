import { BlurChangeDirective } from './blur-change.directive';

describe('BlurChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurChangeDirective();
    expect(directive).toBeTruthy();
  });
});
